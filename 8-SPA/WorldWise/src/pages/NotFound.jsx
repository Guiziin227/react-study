import NavBar from "../components/NavBar.jsx";

const NotFound = () => {
    return (
        <div>
            <NavBar/>
            Página não encontrada :(
        </div>
    );
};

export default NotFound;